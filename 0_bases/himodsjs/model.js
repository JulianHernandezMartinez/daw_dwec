
/**
 * A message to display as welcome
 * @param {string} name - a name to use for the message
 * @returns {string}
 */
function getWelcomeMessage(name) {
    return `Hello, ${name}, welcome to modernity!`;
  }
  
  export { getWelcomeMessage };
  